apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "northstar.fullname" . }}-test-connection"
  labels:
    {{- include "northstar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "northstar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
